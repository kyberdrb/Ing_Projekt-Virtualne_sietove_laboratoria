Install ubuntu server 14.04 x64 as usual
if you can't login follow this guide:
  https://wiki.archlinux.org/index.php/Reset_root_password#Using_GRUB_to_invoke_bash

install unetlab
  curl -s http://www.unetlab.com/rrlabs.key
  curl -s http://www.unetlab.com/install.sh | bash

reboot
done.


Now we need to correct SSH settings:
  vim /etc/ssh/sshd_config

Find and correct these lines (Tip: use '/' in normal mode to search a string from the top of the file):
To enable password authentication, uncomment
  #PasswordAuthentication yes
  
To enable root login, uncomment
  #PermitRootLogin yes
  
To enable ssh key login, uncomment
  #PubkeyAuthentication yes
  #AuthorizedKeysFile .ssh/authorized_keys

Save file and exit from vim.
Restart SSH service:
  service ssh restart

Source:
  http://superuser.com/questions/543626/ssh-permission-denied-on-correct-password-authentication
  https://www.cyberciti.biz/faq/howto-restart-ssh/
  http://vim.wikia.com/wiki/Searching







UTILITIES INSTALLATION
#install guestfish
  apt-get install libguestfs-tools
  update-guestfs-appliance





*********** FOR ASAv **************
Use UltraVNC's vncviewer.exe to connect to the Cisco ASA command line 

It's possible to use telnet instead of vnc, but it didn't work for me:
  cd /opt/unetlab/addons/qemu/asav-941-200/
  guestfish -a virtioa.qcow2
  update-guestfs-appliance
  run
  mount /dev/sda1 /
  touch /use_ttyS0
  umount /
  exit

Sources:
  https://bugs.launchpad.net/fuel/+bug/1467579




********** FOR CISCO 7200 VXR ***************
When creating a new cisco vxr7200 router, add all necessary expansion cards right
on the first configuration screen, otherwise you will not be able to start the router again,
when you add a expansion card aftwerwards.




UNETLAB PROJECTS LOCATION
  /opt/unetlab/labs



**************** FOR ALL QEMU IMAGES *******************
ALL IMAGES HAVE TO HAVE NAME "hda.qcow2" !!!!
OTHERWISE THEY WILL NOT BOOT!

Sources:
  http://noshut.ru/2015/09/how-to-run-juniper-vmx-in-unetlab/
  http://www.unetlab.com/2014/11/adding-cisco-cloud-service-router-csr1000v-images/
  





































OTIONAL AND NOT RECOMMENDED!
edit grub
  1) Edit /etc/default/grub with

  nano /etc/default/grub 

  2) Put 2 new lines and comment 2 old like identical lines

  GRUB_DEFAULT=saved
  #GRUB_TIMEOUT=0

  3) make the "saved" kernel you placed in the /etc/default/grub to unetlab one

  grub-set-default "Ubuntu, with Linux 3.16.7-ckt8-unetlab"

  3) Update grub

  update-grub


  4) Reboot the server and check if network configuration works.
